describe('Happy paths ', () => {
  it.skip(
    'A user is able to visit the homepage and search, later on, to change the page number and go back to the homepage',
  );

  it.skip(
    'A user is able to visit any previously visited search and change the search term, later on, to change the page number',
  );
});

// Prevent TypeScript from reading file as legacy script
export {};
